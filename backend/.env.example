# Django settings
DEBUG=true
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=127.0.0.1,localhost

# CORS settings (for frontend development)
# CORS is configured in settings.py to allow http://localhost:5173 and http://127.0.0.1:5173

# Future examples (not used yet)
# DATABASE_URL=postgres://user:password@localhost:5432/taskroot
