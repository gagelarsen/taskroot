# Contributing to TaskRoot

Thanks for your interest in contributing to TaskRoot!

## Local setup

1. Clone the repo
2. Create and activate a virtualenv
3. Install dev dependencies

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements/dev.txt
pre-commit install
```
